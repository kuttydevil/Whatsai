{
  "name": "whatsapp-automation-client",
  "version": "1.0.0",
  "description": "Client-side web automation with WebAssembly and Gemini API integration",
  "type": "module",
  "scripts": {
    "dev": "vite --port 12000 --host",
    "dev:custom": "node dev-server.js",
    "build": "vite build",
    "build:wasm": "./build-wasm.sh",
    "preview": "vite preview --port 12000 --host",
    "proxy": "cd proxy-server && npm start"
  },
  "dependencies": {
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-top-level-await": "^1.3.1",
    "vite-plugin-wasm": "^3.2.2"
  }
}